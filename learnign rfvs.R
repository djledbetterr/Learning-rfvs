# okay so this script is just me working through and learnign rfvs since there is so much information about this

# lets start with installing this 

sdir = "<FVS-interface>/rFVS/R/R-4.x.x/library/rFVS"
for (rf in dir (sdir)) source (paste(sdir,rf,sep="/"))
